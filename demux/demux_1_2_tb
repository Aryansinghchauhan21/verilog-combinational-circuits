module tb;
reg d, sel;
wire [1:0]y;

demux_1_2 abc(d,sel,y);

initia begin
  $monitor("Time = %0t, d =%b, sel = %b, y=%b",$time,d,sel,y);
for(int i = 0; i < 4; i++)
  begin
   {d,sel} = i;
  #1;
  end
 $finish;
end
  initial begin
  $dumpfile("dump.vcd");
$dumpvars(0,tb);
end
endmodule
  
